import { api } from '@/lib/api'

/**
 * Absensi / Attendance API service.
 */

export const absensiService = {
  // getRecords: (params?: { date?: string; userId?: string }) =>
  //   api('/attendance', { query: params }),
  // checkIn: (data: { userId: string; notes?: string }) =>
  //   api('/attendance/check-in', { method: 'POST', body: data }),
  // checkOut: (recordId: string) =>
  //   api(`/attendance/${recordId}/check-out`, { method: 'PATCH' }),
}

void api
