import { api } from '@/lib/api'

/**
 * GPS API service.
 */
export const gpsService = {
  // getLocations: (userId: string) => api(`/gps/locations/${userId}`),
  // getLiveLocation: (userId: string) => api(`/gps/live/${userId}`),
}

void api
