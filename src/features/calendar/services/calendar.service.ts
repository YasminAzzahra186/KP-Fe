import { api } from '@/lib/api'

/**
 * Calendar API service.
 */
export const calendarService = {
  // getEvents: (params?: { start: string; end: string }) =>
  //   api('/calendar/events', { query: params }),
  // createEvent: (data: Partial<CalendarEvent>) =>
  //   api('/calendar/events', { method: 'POST', body: data }),
}

void api
