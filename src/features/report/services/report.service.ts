import { api } from '@/lib/api'

/**
 * Report API service.
 */
export const reportService = {
  // generate: (filter: ReportFilter) =>
  //   api('/reports/generate', { method: 'POST', body: filter }),
  // export: (filter: ReportFilter, format: 'pdf' | 'csv') =>
  //   api(`/reports/export?format=${format}`, { method: 'POST', body: filter }),
}

void api
