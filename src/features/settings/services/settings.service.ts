import { api } from '@/lib/api'

/**
 * Settings API service.
 */
export const settingsService = {
  // get: () => api('/settings'),
  // update: (data: Partial<AppSettings>) =>
  //   api('/settings', { method: 'PATCH', body: data }),
}

void api
