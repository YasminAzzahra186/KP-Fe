import { api } from '@/lib/api'

/**
 * Timer API service.
 */
export const timerService = {
  // start: (data: { projectId: string; description?: string }) =>
  //   api('/timer/start', { method: 'POST', body: data }),
  // stop: (entryId: string) =>
  //   api(`/timer/${entryId}/stop`, { method: 'PATCH' }),
  // getEntries: (params?: { userId?: string; date?: string }) =>
  //   api('/timer/entries', { query: params }),
}

void api
