import { PlaceholderPage } from '@/shared/components/placeholder-page'
import { Timer } from 'lucide-react'

export function TimerPage() {
  return (
    <PlaceholderPage
      title="Timer"
      description="Track working hours and time entries."
      icon={Timer}
    />
  )
}
